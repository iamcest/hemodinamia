<h5 class="title is-5">New Patient Demographics</h5>
<%= render "form.html", Map.put(assigns, :action,
Routes.pt_path(@conn, :create)) %>

<span><%= link "Back To Patient List", to: Routes.pt_path(@conn, :index), class: "button is-link" %></span>
